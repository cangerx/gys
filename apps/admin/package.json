{
  "name": "@qiye/admin",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port ${ADMIN_PORT:-3100}",
    "build": "vue-tsc --noEmit && vite build",
    "lint": "node -e \"console.log('admin lint not configured yet')\"",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "@qiye/shared": "workspace:*",
    "vue": "^3.5.22",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "typescript": "^5.9.3",
    "vite": "^7.1.7",
    "vue-tsc": "^2.2.12"
  }
}
